# reactive-demo
demo spring service to show reactive api

1. Run the springboot application using mvn spring-boot:run
2. Open command prompt to test following apis:
	a. http://localhost:8080/foo
	b. http://localhost:8080/stringfoo
